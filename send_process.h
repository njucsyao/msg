#ifndef send_process_h
#define send_process_h

#include <stdio.h>
#include "global.h"

void do_send_process(request *self)
{
    if(self->status == 0)
    {
        
    }
    if(self->status == 1)
    {
        
    }
    if(self->status == 2)
    {
        
    }
    if(self->status == 3)
    {
        
    }
}
#endif
